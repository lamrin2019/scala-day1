
object ValVarDemo {
  // val is immutable once declared you cannot change the value
   val a = 100;
  // var are mutable once delcared the values can be changed
  var b = 1000;

  def main(args: Array[String]): Unit = {

    //b = "XXX";
  }
}
